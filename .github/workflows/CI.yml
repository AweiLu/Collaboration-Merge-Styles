name: Build and Test

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v4

      - name: Simulate Build
        run: echo "Building the project... Done."

      - name: Simulate Test
        run: |
          echo "Running unit tests..."
          sleep 5
          echo "All tests passed."